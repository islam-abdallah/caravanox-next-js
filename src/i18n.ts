// This file is intentionally left empty as we're using Next.js's built-in
// internationalization features through the dictionary approach instead of i18next.
// The translations are handled via src/dictionaries/{locale}.json files and
// loaded through src/lib/dictionary.ts